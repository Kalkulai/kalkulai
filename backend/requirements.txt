# --- Web Framework ---
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.9,<1.0

# --- Templates / PDFs ---
jinja2>=3.1.4,<4.0
weasyprint>=61.2,<62.0
striprtf>=0.0.24,<0.1

# -- PDF Rendering stack pins --
pydyf>=0.9,<0.11  # 0.11+ bricht WeasyPrint 61 wegen neuer __init__-Signatur

# --- LangChain-Ökosystem
langchain>=0.3.0,<0.4
langchain-community>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.3
docarray>=0.40.0,<0.41

# --- Vektor-DB / Embeddings / API ---
# sentence-transformers>=3.0.1,<4.0  # Kommentiert: Nur für lokale Embeddings nötig
openai>=1.35.10,<2.0
